# Build output (everything the Makefile generates)
build/

# Object files / static + shared libs (in case built outside build/)
*.o
*.a
*.so
*.dylib

# Executables / test binaries
test_allocator

# macOS debug symbols & cruft
*.dSYM/
.DS_Store

# Editor / tooling
.vscode/
.idea/
*.swp
compile_commands.json
